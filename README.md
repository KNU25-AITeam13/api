# api
2025-2 인공지능 13팀 백엔드 서버

## 설치 방법

이 레포지토리는 서브모듈을 포함하고 있습니다. 클론할 때 서브모듈도 함께 받아야 합니다.

```bash
# 서브모듈을 포함하여 클론
git clone --recurse-submodules <repository-url>

# 이미 클론한 경우 서브모듈 업데이트
git submodule update --init --recursive
```
